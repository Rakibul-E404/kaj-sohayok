///Section : Use Types
enum UserType { normalUser, serviceProvider }

///Section : My Bookings Screen Enums
enum BookingStatusEnum {
  pending,
  acceptedBooking,
  inProgress,
  paymentRequest,
  canceled,
  workCompleted,
}
